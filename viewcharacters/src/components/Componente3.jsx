import React from "react";
import LetterComponent from "./LetterComponent";

const Componente3 = ({ inputText }) => {
  return (
    <div>
      <LetterComponent text={inputText} />
    </div>
  );
};

export default Componente3;
